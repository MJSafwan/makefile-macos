# Makefile MacOS

A simple template for building C projects in MacOS

To initialize the project, run

```sh
make setup
```

To get information about building, run

```sh
make info
```

To clean the build files, run

```sh
make clean
```

